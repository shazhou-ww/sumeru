description: 挂载非法路径 → 400
steps:
- type: exec
  command: sumeru session add test-proto --project /etc/passwd --task 'test' 2>&1; true
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: project_path_out_of_bounds|400|invalid|error|not found
