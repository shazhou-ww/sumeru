description: 按偏移量查询 turns
steps:
- type: exec
  command: SID=$(cat /tmp/send_session_id) && sumeru session turns $SID --offset 0 --format json
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: ses_|turn|role|empty
