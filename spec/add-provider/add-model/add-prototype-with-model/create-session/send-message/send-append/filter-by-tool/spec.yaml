description: 按工具类型过滤 turns
steps:
- type: exec
  command: SID=$(cat /tmp/send_session_id) && sumeru session turns $SID --format json
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: ses_|turn|role|empty
