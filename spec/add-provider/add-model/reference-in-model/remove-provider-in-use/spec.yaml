description: 删除被 model 引用的 provider → 409
steps:
- type: exec
  command: sumeru provider remove test-provider 2>&1; true
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: provider_in_use|409|in use
