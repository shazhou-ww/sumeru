description: 验证纯文本轮次处理
steps:
- type: exec
  command: |
    SID=$(cat /tmp/sarsapa_session_id) && sleep 5 && sumeru session turns $SID --format json
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: 'ses_|turn|role|content'
