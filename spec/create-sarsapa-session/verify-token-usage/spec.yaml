description: 验证 token usage 统计
steps:
- type: exec
  command: |
    SID=$(cat /tmp/sarsapa_session_id) && sleep 5 && sumeru session turns $SID --format json
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: 'ses_|token|usage|turn'
