description: 重启 sumeru server
primary: resume-after-restart
steps:
- type: exec
  command: sumeru server restart
  assert:
    type: regex
    conditions:
    - path: stdout
      regex: restarted|running
