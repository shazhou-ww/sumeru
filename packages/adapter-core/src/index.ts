// @sumeru/adapter-core — adapter common framework (v2 scaffold).
// Placeholder export so the package compiles as a workspace member.
// The cli-kit NDJSON entrypoint and shared adapter runtime land in later M1 issues.
export const VERSION = "0.1.0";
