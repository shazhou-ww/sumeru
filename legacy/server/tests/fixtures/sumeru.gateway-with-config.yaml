name: sumeru@neko

gateways:
  claude-code:
    adapter: claude-code
    config:
      sendTimeoutMs: 1800000
      createSessionTimeoutMs: 300000
      maxTurns: 120
    capabilities:
      resume: true
      streaming: true
