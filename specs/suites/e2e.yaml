suite: e2e
description: 端到端主干流程 — 覆盖 session 创建、model 切换、环境变量展开
prerequisites:
  - Sumeru host running with host.yaml
  - sumeru/hermes:dev image built from latest main
  - LLM endpoint reachable from container
  - SQLite seeded with Provider + Model + Persona

scenarios:
  - session/create-and-start/tc-happy-path-hello-world.md
  - session/create-and-start/tc-env-var-expansion.md
  - resume/model-hot-switch.md
